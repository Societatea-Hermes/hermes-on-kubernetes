apiVersion: apps/v1
kind: Deployment
metadata:
  name: old-hackathon-website
  namespace: apps
  labels:
    app.kubernetes.io/name: old-hackathon-website
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: old-hackathon-website
  template:
    metadata:
      labels:
        app.kubernetes.io/name: old-hackathon-website
    spec:
      containers:
        - name: laravel
          image: ghcr.io/societatea-hermes/hermes-old-hackathon-website:1
          ports:
            - containerPort: 8000